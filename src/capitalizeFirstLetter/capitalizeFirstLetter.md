## Instructions

Write a function that takes in a string as a parameter
and returns the string with every first letter within that string capitalized.

For example, `my favourite Colour is red`

should return `My Favourite Colour Is Red`
